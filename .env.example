PORT=8000
QUERY_DEFAULT_LIMIT=100
RESULTS_PREVIEW_LIMIT=1000
HISTORY_MAX_ENTRIES=10000
DB_STATS_POLL_INTERVAL_SECONDS=30

# Directory where connections and history are stored.
# Default: ~/.sqlworkbench  (or /root/.sqlworkbench in containers without HOME)
# SQLWORKBENCH_DATA=/data/sqlworkbench
